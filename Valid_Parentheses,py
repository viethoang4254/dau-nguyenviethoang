class Solution:
    def isValid(self, s):
        stack = []
        mp = {')':'(', ']':'[', '}':'{'}
        for c in s:
            if c in mp.values():
                stack.append(c)
            else:
                if not stack or stack[-1] != mp[c]:
                    return False
                stack.pop()
        return not stack
